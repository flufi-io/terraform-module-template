---
name: terratest

on:
  pull_request:
    branches:
      - '*'

permissions:
  id-token: write
  contents: read

jobs:
  terratest:
    uses: flufi-io/workflows/.github/workflows/terratest.yaml@main
    secrets: inherit
